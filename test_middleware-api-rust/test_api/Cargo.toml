[package]
name = "test_api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.6"
hyper = { version = "0.14", features = ["server", "http1", "http2"] }
tokio = { version = "1", features = ["full"] }