{
  "name": "circle-area-app",
  "version": "1.0.0",
  "description": "A Node.js app to calculate the area of a circle with a frontend, Cucumber.js, and Playwright tests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npx cucumber-js && npx playwright test"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@cucumber/cucumber": "11.2.0",
    "@playwright/test": "^1.40.0",
    "chai": "^4.3.7",
    "cucumber-html-reporter": "^7.2.0",
    "puppeteer": "^21.0.0"
  }
}
